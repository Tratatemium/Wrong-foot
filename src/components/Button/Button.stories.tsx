import { Button } from "./Button";

const meta = {
  title: "components/Button",
  component: Button,
};

export default meta;

export const Default = {
  args: {
    variant: "default",
  },
};

export const Neutral = {
  args: {
    variant: "neutral",
  },
};

export const Subtle = {
  args: {
    variant: "subtle",
  },
};
